# <Your Folder Name>

> One-line pitch of what this folder does.

## 20-minute quickstart
1. **Install** — copy this folder somewhere on your machine.
2. **Teach it** — open `setup.md` and fill in `memory/profile.md`.
3. **First win** — ask Claude Cowork: _"<the same-day-win prompt>"_.
4. **Activate** — create a scheduled task from `schedules/` for the morning brief.

## What's inside
- `CLAUDE.md` — how this folder operates (its instructions).
- `skills/` — the playbooks it can run.
- `schedules/` — prompts to run on a timer.
- `templates/` — documents it produces.
- `memory/` — **your** data (profile, decisions, people, logs).
- `reports/` — generated outputs.

## Connectors
See `connectors.md` for what to enable in Claude Cowork.
