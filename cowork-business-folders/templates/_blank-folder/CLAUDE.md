# <Your Folder Name> — operating instructions

## Role
Describe who you (Claude) are for this business in 1–2 sentences.

## Hard rules
- Never invent facts or numbers. If a figure isn't in `memory/` or a connector, ask.
- Never send anything external (email, message, filing) without showing a draft first.
- <Add the business's own non-negotiables here.>

## How this folder works
- `memory/` is the source of truth about this business. Read it before acting.
- Write durable facts to `memory/` (profile, decisions, people); write deliverables to `reports/`.
- Skills live in `skills/`; run the one that matches the request.
- Scheduled prompts live in `schedules/`.

## Voice & output
- Tone: <e.g. concise, plain-English, owner-facing>.
- Default format for reports: short headline + bullets + a clear "what needs your decision" section.
